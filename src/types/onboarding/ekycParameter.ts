export interface ChecklistStageParameter {
  id: number;
  code: string;
  label: string;
}

export interface ChecklistStatusParameter {
  id: number;
  code: string;
  label: string;
}

export interface CountryParameter {
  id: number;
  code: string;
  name: string;
}

export interface CustomerTypeParameter {
  id: number;
  code: string;
  name: string;
}

export interface GeneralCheckingParameter {
  id: number;
  key: string;
  label: string;
}

export interface LegalEntityCharacteristicParameter {
  id: number;
  code: string;
  label: string;
}

export interface LegalEntityTypeParameter {
  id: number;
  code: string;
  label: string;
}

export interface LineOfBusinessParameter {
  id: number;
  code: string;
  label: string;
}